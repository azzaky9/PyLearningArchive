# from calculation.calc import add, times, divide, minus
import calculation.calc as c
import calc_age.calc_and_printout as cp

def main():
  # x = 10
  # y = 20
  # totalAdd = c.add(x, y)
  # totalTimes = c.times(x, y)
  # totalDivide = c.divide(x, 2)
  # totalMinus = c.minus(x, y)

  cp.calc_and_printout(15, 4)


  # print(f"total calculation add is {totalAdd}")
  # print(f"total calculation times is {totalTimes}")
  # print(f"total calculation divide is {totalDivide}")
  # print(f"total calculation minus is {totalMinus}")
  

# __name__ berguna untuk memvalidasi nama file
if __name__ == "__main__":
  main() 